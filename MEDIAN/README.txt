Exported from RBG Annotation Studio
Project: MEDIAN
Images: 28 (labeled only: True)
Classes: physical median

images/<split>/     — original image files
labels/<split>/     — YOLO segmentation .txt (one per image)
annotations_coco.json — COCO format with masks/polygons
classes.txt         — one class name per line
data.yaml           — YOLO dataset config
